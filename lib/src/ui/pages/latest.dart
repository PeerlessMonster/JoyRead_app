import 'package:flutter/material.dart';

import '../features/latest/views/latest_screen.dart';

class LatestPage extends StatelessWidget {
  const LatestPage({super.key});

  @override
  Widget build(BuildContext context) => LatestScreen();
}
